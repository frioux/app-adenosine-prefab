#!/usr/bin/env perl

# PODNAME: adenosine

use strict;
use warnings;

use rlib;

use App::Adenosine;

App::Adenosine->new({
   argv => \@ARGV,
   plugins => [
      #'::Stopwatch',  # Adds timing info to -v
      #'::Rainbow',    # Adds color coding to -v
   ],
});
